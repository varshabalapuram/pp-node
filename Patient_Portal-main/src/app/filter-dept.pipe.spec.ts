import { FilterDeptPipe } from './filter-dept.pipe';

describe('FilterDeptPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDeptPipe();
    expect(pipe).toBeTruthy();
  });
});
